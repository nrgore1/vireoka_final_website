/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vercel handles optimization automatically. 
  // You do not need the withHostSafeConfig wrapper.
  reactStrictMode: true,
  // If you were using specific rewrites or redirects in base.js, 
  // move them here.
};

module.exports = nextConfig;